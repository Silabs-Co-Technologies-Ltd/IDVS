# Vercel installs this file for the optional serverless upload form.
# Keep it intentionally empty: api/index.py and idvs.wsgi use only the Python
# standard library so the deployed function stays below Vercel's bundle limit.
#
# Install the offline kiosk dependencies from requirements-offline.txt instead.
